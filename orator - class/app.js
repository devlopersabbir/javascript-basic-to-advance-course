console.clear();
("use strict");
const fNumber = 50;
const lNumber = 30;
// Operator
/**
 * Arithmetic -> +,-,*,/
 * Comparison -> ==, != , ! =, >, <
 * logical -> &&, ||, !
 * Assignment -> +=, -=, *=, /=
 */
// Arithmetic Operator
const arithResult = fNumber * lNumber;
console.log("Arithmetic: ", arithResult);
// Comparison
const comparisonResult = fNumber > lNumber;
console.log("Comprasion: ", comparisonResult);
// Logical
const logicalResult = fNumber && lNumber;
console.log("Logical: ", logicalResult);
// Assignment
